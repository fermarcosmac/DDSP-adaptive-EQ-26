name: ddsp-are
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.1
  - torchaudio>=2.1
  - torchvision
  - pytorch-cuda=12.1
  - numpy>=1.24
  - scipy>=1.11
  - matplotlib>=3.7
  - tqdm>=4.65
  - soundfile>=0.12
  - pip
  - pip:
      - auraloss
